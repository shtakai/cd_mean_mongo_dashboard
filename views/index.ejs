<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title></title>
</head>
<body>
  <h2>Mongooses</h2>
  <% if(mongooses){ %>
    <ol>
      <% for(var m of mongooses) { %>
        <li>
          <a href="/mongooses/<%= m.id %>">
            <%= m.name %>
          </a>
        </li>
      <% } %>
    </ol>
  <% } %>
</body>
</html>
